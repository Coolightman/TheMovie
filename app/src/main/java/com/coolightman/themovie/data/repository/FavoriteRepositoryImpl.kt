package com.coolightman.themovie.data.repository

import com.coolightman.themovie.domain.repository.FavoriteRepository
import javax.inject.Inject

class FavoriteRepositoryImpl @Inject constructor() : FavoriteRepository {

    override fun addMovieToFavorite(movieId: Long) {
        TODO("Not yet implemented")
    }

    override fun removeMovieFromFavorite(movieId: Long) {
        TODO("Not yet implemented")
    }
}